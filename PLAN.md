# FPS ARENA X — план работ

**База:** процедурный движок Three.js r180 (11 подсистем, всё генерируется в рантайме).
**Слой:** механики и арсенал из FPS Arena (боты / 1×1 / 10×10, 9 стволов, съёмные прицелы, лазер, фонарь, глушитель, доска оружия).

## Принцип интеграции — «не ломать базу»

База написана под жёсткий контракт (`ARCHITECTURE.md`): подсистема
владеет своей папкой, никто не импортирует чужие модули, всё общение — через
`ctx.get(id)` и события. Поэтому новый код **не переписывает** `src/weapons`,
`src/ai`, `src/ui`, а добавляет свои подсистемы и расширяет базовые в рантайме:

| новая подсистема | id | что делает |
|---|---|---|
| `src/arsenal/` | — (кит, не подсистема) | 9 стволов FPS Arena на геометрическом ките M4A1 + съёмное навесное |
| `src/modes/` | `modes` | режимы: боты (DM/CTF, 3 сложности), 1×1 (bo5), 10×10 |
| `src/net/` | `net` | WebSocket-клиент протокола FPS Arena (лобби, комнаты, реконнект) |
| `src/shell/` | `shell` | меню, доска оружия (gunsmith), табло, итоги матча |
| `server/` | — | FastAPI-релей из FPS Arena, раздаёт собранный билд |

## Этапы

- [x] **0. Разведка.** Прочитан контракт базы, API `weapons`/`ai`/`ui`/`player`/`physics`,
      модель `models/rifle.js` (M4A1) как эталон, статы 9 стволов из `game.html`.
- [x] **1. Данные арсенала.** `arsenal/defs.js` — 9 стволов в схеме базы
      (rpm, баллистика, отдача-паттерн, поза ADS). `arsenal/attachments.js` —
      каталог навесного и чистая функция пересчёта статов.
      *Тест:* `tools/verify-arena.mjs --unit` — схема, диапазоны, совместимость слотов.
- [x] **2. Модели стволов.** `arsenal/models/*.js` на ките `weapons/parts.js`
      (ресиверы, стволы, ДТК, цевья, магазины, оптика) — AK-паттерн, M416, SCAR-H,
      СВД, MP5, M870, Glock-18, Desert Eagle.
      *Тест:* сборка каждой модели, наличие всех обязательных `nodes`, бюджет треугольников.
- [x] **3. Съёмное навесное.** Планки, быстросъём оптики (iron/red dot/holo/3x/ПСО),
      глушитель, лазер (луч + точка), фонарь (SpotLight в viewScene), передняя рукоять.
      *Тест:* монтаж/демонтаж без утечек геометрии, пересчёт статов, стабильность числа источников света.
- [x] **4. Доска оружия (gunsmith).** Верстак в мире + экран настройки ствола.
- [x] **5. Режимы.** Боты (DM/CTF, сложности), 1×1 bo5, 10×10 с автобалансом.
- [x] **6. Сеть.** Клиент протокола FPS Arena + порт сервера.
- [x] **7. Дизайн-слой.** Меню/HUD/итоги: собственная типографика, сетка, motion.
- [x] **8. Общий тест.** `tools/verify-all.mjs` — сквозные проверки между слоями
      (регистрация подсистем, покрытие гейтами, честность плана, собираемость).
      Сборка `npm run build` и запуск сервера — в CI на GitHub Actions,
      потому что в песочнице нет сети и нет `three`/`vite`.

## Правила качества (перенесены из базы + дизайн-гайд)

1. Никакого `Math.random()` в геймплее — только `ctx.rng`.
2. Нулевые аллокации в кадре: всё преаллоцировано в `init()`.
3. Всё созданное освобождается в `dispose()`.
4. Число видимых point-light'ов постоянно (иначе рекомпиляция шейдеров).
5. Дизайн: без дефолтного «ИИ-стиля» — никаких фиолетовых градиентов, стоковых
   glassmorphism-карточек и центрированных hero-блоков. Своя сетка, свой ритм,
   анимация только на трансформациях и прозрачности.
