# FPS ARENA

Браузерный тактический шутер. Автор и владелец — [918web](https://github.com/918web).

Три режима: бой с ботами (до 60 противников, три сложности, дефматч и захват
флага), дуэль 1×1 до пяти побед, командный бой 10×10 с автобалансом.

Девять стволов: АКМ, АК-74, M416, SCAR-H, СВД, MP5, M870, Glock-18, Desert Eagle.
Съёмная обвеска: пять типов прицелов, глушитель, дульные устройства, лазер, фонарь,
передняя рукоять, сошки, три варианта магазинов.

## Состояние

| Этап | Что | Статус |
| --- | --- | --- |
| 1 | Данные арсенала: баллистика, отдача, каталог обвеса | готово |
| 2 | Модели девяти стволов | готово |
| 3 | Съёмное железо в сцене: прицелы, глушитель, лазер, фонарь | готово |
| 4 | Доска с оружием и экран сборки | готово |
| 5 | Режимы: боты, 1×1, 10×10 | готово |
| 6 | Сеть и сервер | готово |
| 7 | Меню выбора режима, иконка, экран результатов | готово |
| 8 | Общий тест и сборка в CI | готово |

В репозитории лежит только новый слой игры (`src/arsenal`, `src/modes`, `src/net`,
`src/shell`, `server/`, `tools/verify-*`). Базовый движок — 146 файлов рендера,
физики, мира, ai, ui и звука — ещё не залит, без него `npm run build` не соберётся.
Полный исходник целиком собирается в архив `fps-arena-x.zip`.

## Как устроен арсенал

Геометрия разделена на две части, и это сделано намеренно:

- `src/arsenal/models/specs.js` — чистые числа и чистые функции. Ресивер, ствол,
  цевьё, магазин, приклад, линия прицеливания и все точки крепления. Здесь нет
  three.js, поэтому всю разметку девяти стволов можно проверить тестами без браузера
  и без видеокарты.
- `src/arsenal/models/build.js` — превращает одну спецификацию в реальную геометрию.

Семь разных архитектур, а не один силуэт с разными цифрами: `ar` (раздельные
ресиверы), `ak` (газовая трубка над стволом, крышка, боковая планка),
`battle` (монолит и складной приклад), `dmr` (длинный ствол, дерево, рамочный
приклад), `smg` (трубчатый ресивер), `pump` (подствольный магазин, подвижное
цевьё на тягах), `pistol` (затвор на рамке).

## Режимы и сеть

`src/modes/rules.js` — таблицы без three.js: три сложности, четыре волны гарнизона,
константы захвата флага, таблица очков. `src/modes/match.js` — машина матча
(раунды, серии, автобаланс, MVP), тоже без движка — тест прогоняет целый
десятиминутный матч в node.

`src/net/protocol.js` — словарь протокола и конструкторы кадров,
`src/net/index.js` — сокет, реконнект и чужие игроки. Режим с ботами сокет не
открывает вообще. Состояние уходит 20 раз в секунду, чужие игроки
интерполируются на 100 мс позади свежего кадра.

`server/main.py` — FastAPI-релей: лобби, комнаты, реконнект и раздача собранного
билда. Счёт и конец матча считает сервер, а не клиент.

## Меню и запуск матча

Меню рисуется до того, как загружается three и строится движок: на холодном
бесплатном дино первый ответ идёт почти минуту, и всё это время игрок должен
видеть лицо игры, а не чёрный холст. Выбор режима заодно решает, как собирать
движок — качество, размер гарнизона и открывать ли сокет вообще.

Экран итогов один на все три режима: он читает снимок матча
(`modes:over`), а не режим, поэтому в нём нет ни одной ветки «если 1×1».

## Тесты

```
npm test
```

Семь гейтов, цепочкой через `&&` — первый же провал останавливает прогон:

| Гейт | Что стережёт | Проверок |
| --- | --- | --- |
| `verify-arena` | архитектура, изоляция подсистем, данные арсенала | 772 |
| `verify-hardware` | съёмное железо: прицелы, глушитель, лазер, фонарь | 71 |
| `verify-gunsmith` | доска с оружием и экран сборки | 24 |
| `verify-modes` | правила матча, счёт, раунды, волны ботов | 45 |
| `verify-net` | протокол, интерполяция, реконнект, сервер | 55 |
| `verify-shell` | меню выбора режима, порядок загрузки, вкладка | 23 |
| `verify-all` | стыки между слоями: что ничего не забыто подключить | 32 |

Всего 1022 проверки, все статические: читают исходник, импортируют чистые модули
и прогоняют матчи в обычном node. Ни GPU, ни браузер, ни сеть не нужны.

Сборка (`npm run build`), импорт релей-сервера и проверка его маршрутов идут
отдельно — в GitHub Actions (`.github/workflows/ci.yml`), на чистом клоне.

## Запуск

```
npm install
npm run dev
```

С сервером:

```
npm run build
pip install -r server/requirements.txt
cd server && uvicorn main:app --port 8000
```

Движок рендера и набор деталей для моделей — открытая база под лицензией MIT;
её файл `LICENSE` едет вместе с кодом движка, как требует лицензия. На саму игру,
механики, арсенал, режимы и дизайн это не влияет.
